@printf(x: str): int
@exit(x: int)

fc main
    loop i, v in "IterThroughMe"
        .1 / 0.1
        printf("OWO\n")
    end

    add(1.0, .1)

    printf("Hi")
end

fc add(a: float, b: float): float
    return a + b
end